# Pixel Guild Command Implementation Plan

1. Add an original generated pixel-art party sprite sheet to static assets.
2. Rebuild the operator surface as a responsive mission-control layout while preserving its existing endpoints and element IDs.
3. Map real `RunPhase` values to the appropriate role card and retain SSE-driven lifecycle, approval, history, and audit behavior.
4. Verify static asset serving, JavaScript syntax, focused web tests, and the full Gradle suite.
